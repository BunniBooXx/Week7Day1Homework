function moveZeros(arr) {
      arr.push(0)
      console.log(arr)
    }
   
    let dog_string = "Hello Max, my name is Dog, and I have purple eyes!"
    let dog_names = ["Max","HAS","PuRple","dog"]
    let dog_words = dog_string.toUpperCase 
    
    function findWords(){
        for (i in dog_names){
            ([i]).toUpperCase
        } 
        for (i in dog_words){
           ([i]).spilt(' ')
        }
         if ( i in dog_words == i in dog_names){
         console.log("Matched dog_name")
        }
              else { 
          console.log("No Matches")
    }
    }













    given_arr = ["Max","Baseball","Reboot","Goku","Trucks","Rodger"]
output_arr=[]


function replaceEvens(arr){
    if (Array.length === 0){
        return [];
    }
    return output_arr = given_arr.filter(function(index) {
      for (index in given_arr) { 
        if (index % 2 === 0){
        given_arr.splice(0,2, ' even index ')
        console.log(given_arr)
    }
    
    

}

})

    

console.log(replaceEvens(given_arr))